name: tests-auth

on:
  pull_request
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Experiment
        run: |
          pwd
          mkdir hello
          cd hello
          pwd
      - name: Test
        run: |
          cd auth
          npm install
          npm run test:ci
